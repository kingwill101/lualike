# Basic LOVE entrypoints

These are intentionally tiny entrypoints for smoke tests and manual boot checks.

- `hello_world/main.lua` — load/update/draw baseline
- `counter/main.lua` — state mutation across frames
- `input_probe/main.lua` — keyboard/mouse/touch callbacks
